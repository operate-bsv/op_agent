defmodule FBAgent.Cache.NonCache do
  @moduledoc """
  TODOC
  """


  #with {:ok, tx}    <- apply(cache, :fetch_tx, [txid, tape_adapter, cache_options]),
  #       tape         <- Tape.from_bpu(tx),
  #       {:ok, procs} <- apply(cache, :fetch_procs, [Tape.procedure_refs(tape), proc_adapter, cache_options]),
  #       tape         <- Tape.apply_procs(tx)

  
end